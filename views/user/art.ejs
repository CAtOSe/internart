<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>InternArt</title>
  <link rel="stylesheet" type="text/css" href="/assets/style/main.min.css" />
  <link rel="stylesheet" type="text/css" href="/assets/style/gallery.min.css" />
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
  <script src="https://code.jquery.com/jquery-3.3.1.min.js" integrity="sha256-FgpCb/KJQlLNfOu91ta32o/NMZxltwRo8QtmkMRdAu8=" crossorigin="anonymous"></script>
  <script src="/assets/js/popups.js"></script>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
  <%- include('../topNav.ejs') %>

  <main id="art" style="background-color:#<%= artwork.bgColor %>">
    <img id="art-img" src="<%= artwork.path %>" />
    <div class="art-info">
      <div>
        <span class="title">
          <%= artwork.title %>
          <% if (userID == artwork.ownerID) { %>
            <a id="edit" class="delink pointer"><i class="material-icons">edit</i></a>
          <% } %>
        </span>
        <div>
          <span class="artist"><a class="delink" href="/u/<%= artwork.ownerID %>"><img src="/assets/user.png" /><%= artwork.ownerName %></a></span>
        </div>
      </div>
      <span class="votes"><i class="material-icons pointer">thumb_up</i><%= artwork.votes %></span>
      <% if (!(artwork.description == null || artwork.description == "" || artwork.description == undefined)) { %>
        <span class="label">description</span>
        <p><%= artwork.description %></p>
      <% } %>
    </div>
  </main>
  <% if (userID == artwork.ownerID) { %>
    <script>
      $('#edit').on('click', function() {
        window.location.replace("/edit/<%= artwork.id %>");
      });
    </script>
  <% } %>
</body>
</html>
